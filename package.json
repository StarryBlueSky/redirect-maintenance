{
  "private": true,
  "name": "redirect-maintenance",
  "version": "1.0.0",
  "description": "302 redirect to the maintenance page when the origin server of `*.starry.blue` is not available.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "SlashNephy <spica@starry.blue>",
  "devDependencies": {
    "prettier": "2.6.2"
  }
}
